import java.awt.*;

public class gBlockCollision extends gBlock {
    public gBlockCollision(String id, String prefabId, int x, int y, int w, int h) {
        super(id, prefabId, "BLOCK_COLLISION", x, y, w, h);
    }
}
