unbind all
bind - -zoom
bind = zoom
bind , -e_nextthing
bind . e_nextthing
bind backspace e_delthing
bind b e_pretile
bind n e_nextile
bind m e_deltile
bind h e_preprop
bind j e_nexprop
bind k e_delprop
bind [ e_preflare
bind ] e_nexflare
bind \ e_delflare
bind space jump
bind ctrl crouch
bind w playerup
bind s playerdown
bind a playerleft
bind d playerright
bind tab showscore
bind f flashlight
bind enter activateui
bind escape gobackui
bind up selectup
bind down selectdown
bind left selectleft
bind right selectright
bind r reload
bind e centercamera
bind 1 slot0
bind 2 slot1
bind 3 slot2
bind 4 slot3
bind 5 slot4
bind y chat
-bind ` console
-bind ctrl -crouch
-bind w -playerup
-bind s -playerdown
-bind a -playerleft
-bind d -playerright
-bind tab -showscore
-bind up -selectup
-bind down -selectdown
-bind left -selectleft
-bind right -selectright
debug 1