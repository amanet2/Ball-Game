unbind all
bind - exec scripts/zoomout
bind = exec scripts/zoomin
bind backspace e_delthing
bind w playerup
bind s playerdown
bind a playerleft
bind d playerright
bind tab exec scripts/showscore
bind enter activateui
bind escape gobackui
bind up selectup
bind down selectdown
bind left selectleft
bind right selectright
bind y chat
-bind ` console
-bind w -playerup
-bind s -playerdown
-bind a -playerleft
-bind d -playerright
-bind tab exec scripts/showscorex
-bind up -selectup
-bind down -selectdown
-bind left -selectleft
-bind right -selectright
-bind r e_rotthing
